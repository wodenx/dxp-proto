{transformer=FROM_LANDSCAPE}
|id                    |#{generate(regexify '[a-zA-Z0-9]{16}')}           |
|name                  |Product #{generate(regexify '[a-zA-Z0-9]{8}')}    |
|slug                  |#{generate(regexify '[a-z]{16}')}                 |
|directions            |#{trim(#{generate(regexify '[a-zA-Z0-9 ]{256}')})}|
|warnings              |#{trim(#{generate(regexify '[a-zA-Z0-9 ]{256}')})}|
|additional_information|#{trim(#{generate(regexify '[a-zA-Z0-9 ]{256}')})}|
|sku                   |#{generate(regexify '[a-zA-Z0-9]{64}')}           |
|upc                   |#{generate(regexify '[a-zA-Z0-9]{64}')}           |
|ean                   |#{generate(regexify '[a-zA-Z0-9]{64}')}           |
|summary_plain         |#{trim(#{generate(regexify '[a-zA-Z0-9 ]{64}')})} |
|summary_bold          |#{trim(#{generate(regexify '[a-zA-Z0-9 ]{64}')})} |
|summary_superscript   |#{trim(#{generate(regexify '[a-zA-Z0-9 ]{64}')})} |
|summary_link_text     |#{trim(#{generate(regexify '[a-zA-Z0-9 ]{16}')})} |
|summary_link_url      |https://example.com/                              |
