{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
      "build": {
        "outputs": ["public/**", "lib/**", ".env.production", ".env.development", ".vercel/**"],
        "dependsOn": ["^build"]
      },
      "check": {},
      "dev": {
        "persistent": true
      }
    }
  }
