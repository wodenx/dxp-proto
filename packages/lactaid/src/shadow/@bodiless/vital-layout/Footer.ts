import { vitalFooterBase, asFooterToken } from '@bodiless/vital-layout';
import lactaidFooter from '../../../components/Layout/Footer/tokens';

const Default = asFooterToken(lactaidFooter.Default);

export default {
  ...vitalFooterBase,
  Default,
};
