import { vitalHeaderBase, asHeaderToken } from '@bodiless/vital-layout';
import lactaidHeader from '../../../components/Layout/Header/tokens';

const Default = asHeaderToken(lactaidHeader.Default);

export default {
  ...vitalHeaderBase,
  Default,
};
